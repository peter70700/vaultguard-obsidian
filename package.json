{
  "name": "vaultguard-monorepo",
  "version": "1.2.9",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "license": "SEE LICENSE IN LICENSE",
  "scripts": {
    "build": "npm run -w vaultguard-sync build",
    "test": "npm run -w vaultguard-sync test",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "typecheck": "tsc -noEmit -skipLibCheck"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.15"
  }
}
